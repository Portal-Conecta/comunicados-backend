package com.portal.conecta.comunicados.module.comunicado.domain.enums;

/**
 * Turnos do Core (sem endpoint dedicado). Espelha o enum {@code Shift} do Hub/Core.
 */
public enum ShiftCode {
    FULL_AM_PM,
    FULL_PM_NT
}
